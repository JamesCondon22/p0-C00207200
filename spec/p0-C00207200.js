/* global p0Studentid, describe, it, expect, should */

describe('p0-C00207200()', function () {
  'use strict';

  it('exists', function () {
    expect(p0-C00207200).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});
